# Point the frontend at your running FastAPI backend.
# If you leave this unset, the app runs fully in the browser (localStorage),
# which is great for a quick demo but data lives only on that one device.
NEXT_PUBLIC_API_URL=http://localhost:8000
